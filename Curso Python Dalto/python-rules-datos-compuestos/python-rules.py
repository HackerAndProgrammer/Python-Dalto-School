#Indice = Posicion
#ej: 
#lista = ["batman", "spiderman"]
    
#elemento[1] = "Batman" = posicion 0

#elemento[2] = "Spiderman" = posicion 1

#lista = []

#tupla = ()

#conjunto = {}

#diccionario = {
    
#}

#print(lista[#x])

#print(tupla[#x])

#print(conjunto[#x])

#print(diccionario[#'x'])

#<diccionario> -> key - value

#'nombre' : "Julian"

#'nombre' = key

#"Julian" = value

#comas = elementos - 1

#operadores logicos (AND)

# (-1-) = False, operacion devuelve False

#operadores logicos (OR)

# (-1-) = True, operacion devuelve True

#operadores logicos (NOT)

#Devuelve x-><-

#funciones
print() #le dice al programa que se va a ejecutar en la consola
type()#le dice al programa que tipo de dato es (string, INT, FLOAT, BOOL, LIST, TUPLEX, SET, DICT)

tipo_de_dato = type(True)
print(tipo_de_dato)


#Metodos de String

#Dir (funcion)
#muestra todo lo que podemos hacer con el argumento
#upper
#pasa todo a mayuscula                           all of this _/ -> Data_type = String
#lower
#pasa todo a  minuscula
#capiitalize
#es como un lower, pasa TODO a minuscula, y despues toma la PRIMERA LETRA y la convierte a mayuscula
#find
#encuentra datos en cadenas, pero, si no hay resultados validos(coincidencias), retorna en  -1
#index
#encuentra datos en cadenas, pero, si no hay resultados validos(coincidencias), retorna en  una excepcion
#isnumeric
#revisa si la variable es numerica, si lo es, el programa retorna True, si no, False
#isalpha
#revisa que la variable no tenga espacios, ni caracteres especiales, ni numeros (alfanumericos), si no contiene, el programa retorna True, si no, False
#COUNT
#revisa que la variable contenga coincidencias, si las tiene, el programa retorna la cantidad de las veces que se dan las coincidencias, si no, retorna 0
#LEN
#cuenta los caracteres que tiene la variable, y retorna la cantidad
#startswith
#verifica si la variable empieza con otra string
#endswith
#verifica si la variable termina con otra string
#replace
#remplaza una string dada por otra dada ( mediante parametros)
#split
#separa las strings con la string que le establezcamos al programa

#Lists methods
#list
#crea una lista con los parametros que le programamos al programa, devolviendonos una matriz
#pop
#para quitar con un objeto (random.pop(parametro)), si usamos positivos, se cuentan como caracteres de programacion. Si no, (o sea, como negativos) se cuentan como numeros normales

#metodos son:
#dato.metodo()